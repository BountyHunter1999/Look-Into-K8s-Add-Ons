apiVersion: v1
kind: Service
metadata:
  name: {{ include "create-node.fullname" . }}
  labels:
    {{- include "create-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "create-node.selectorLabels" . | nindent 4 }}
